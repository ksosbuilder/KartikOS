import { describe, it, expect } from "vitest";

// Sample unit test to prove the test runner is wired. Replace with real
// tests as features land in Phase 1+.

describe("smoke", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
