// Vitest setup. Runs before every test file. Extend with @testing-library/jest-dom
// matchers when we add a UI component test, or env mocks when we add server tests.

import "@testing-library/react";
